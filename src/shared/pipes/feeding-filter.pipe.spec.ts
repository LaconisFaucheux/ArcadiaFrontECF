import { FeedingFilterPipe } from './feeding-filter.pipe';

describe('FeedingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FeedingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
